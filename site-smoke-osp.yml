---
- hosts: workstation
  gather_facts: false
  tasks:
  - name: OpenStack servers
    os_server_info:
     cloud: ospcloud
     server: frontend
    register: result
    tags:
      - osp.smoke

  - name: Curl website
    uri:
      url: "http://{{ result.ansible_facts.openstack_servers[0].accessIPv4 }}"
      return_content: yes
      status_code: 200
    register: webpage
    failed_when: "'Ansible has done its job' not in webpage.results[0].content"
